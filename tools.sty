\ProvidesPackage{tools}

% Import the standard UTF-8 character set and set margins
\usepackage[utf8]{inputenc}
\usepackage[margin=0.75in]{geometry}

% Needed at one point to make nice backslashes in teletype font
\usepackage{url}

% Allows for Excel-style row combinations
\usepackage{multirow}

% Customized date format that follows the international system
\usepackage[nodayofweek]{datetime}
\newdateformat{thedate}{\THEDAY{ }\monthname[\THEMONTH] \THEYEAR}

% Allow for teletype-bold font to be an option
\DeclareFontShape{OT1}{cmtt}{bx}{n}{<5><6><7><8><9><10><10.95><12><14.4><17.28><20.74><24.88>cmttb10}{}

% Import a special math package for certain symbols
\usepackage{amssymb}

% Import and customize enumeration for easy reading and better organization
\usepackage{enumerate}
\renewcommand{\labelitemi}{$\bullet$}
\renewcommand{\labelitemii}{$-$}
\renewcommand{\labelitemiii}{$\circ$}
\renewcommand{\labelitemiv}{$\square$}

% Allow linking between sections as well to external sources (e.g. email)
\usepackage{hyperref}

% Import code-writing/syntax-highlighting, set default language to OCaml
% Escape-sequencing out of code into standard LaTeX is done using banana brackets (|...|)
\usepackage{listings}
\lstset{frame=none,
  language=Caml,
  aboveskip=0mm,
  belowskip=-\baselineskip,
  showstringspaces=false,
  keepspaces,
  columns=fullflexible,
  escapeinside={(|}{|)},
  basicstyle=\ttfamily,
  numbers=none,
  numberstyle=\small,
  numbersep=5mm,
  keywordstyle=\bfseries,
  commentstyle=\itshape,
  stringstyle=,
  breaklines=true,
  breakatwhitespace=true,
  tabsize=3,
  literate={~} {$\mathtt{\sim}$}{1}
}

% Easy-access command aliases that change languages easily, as needed
\newcommand{\java}{\lstset{language=Java}}
\newcommand{\cee}{\lstset{language=C}}
\newcommand{\ocaml}{\lstset{language=Caml}}
